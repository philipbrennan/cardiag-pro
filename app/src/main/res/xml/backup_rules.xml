<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Exclude database files from backup (they can be large) -->
    <exclude domain="database" path="." />
    <!-- Include preferences -->
    <include domain="sharedpref" path="." />
</full-backup-content>
